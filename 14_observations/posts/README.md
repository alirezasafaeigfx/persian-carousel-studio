# Observation Records

Store compact real-production records here as:

`OBS-NNN-<slug>.md`

Do not backfill old posts from memory simply to create history.

Use `../post-observation-template.md`.

Individual records are **cold memory** and must not be loaded during normal carousel generation.
